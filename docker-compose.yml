services:
  web:
    image: nginx:alpine
    container_name: course-enrollment-preview
    ports:
      - "8080:80"
    volumes:
      - ./online:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped